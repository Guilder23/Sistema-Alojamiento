from django.urls import path
from . import views

app_name = 'reportes'

urlpatterns = [
    # Reportes y Estadísticas - Será ampliado con vistas específicas
    # path('ocupacion/', views.ReporteOcupacionView.as_view(), name='reporte_ocupacion'),
    # path('ingresos/', views.ReporteIngresosView.as_view(), name='reporte_ingresos'),
    # path('clientes/', views.ClientesFrecuentesView.as_view(), name='clientes_frecuentes'),
]
